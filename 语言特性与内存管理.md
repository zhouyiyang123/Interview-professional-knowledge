## C++ 语言特性与内存管理

### 指针与引用

sizeof(指针) 在32位系统下永远是 4字节，64位系统下永远是 8字节；而 sizeof(引用) 得到的是被引用对象本身的大小。

指针可以为 nullptr 且可重新赋值，引用声明时必须初始化且不可重新绑定。

### 内存对齐 (Memory Alignment)

结构体大小必须是最大成员变量大小（或指定对齐数）的整数倍。

例题技巧： 注意变量声明顺序，将相同字节大小的变量放一起可以节省内存（如 char a; short b; char c; 占6字节，而 char a; char c; short b; 只占4字节）。

### 智能指针 (Smart Pointers)

std::unique_ptr：独占所有权，不可拷贝，只能移动 (std::move)。

std::shared_ptr：共享所有权，内部通过引用计数管理，拷贝时计数 $+1$，

销毁时 $-1$。std::weak_ptr：不增加引用计数，专门用来解决 shared_ptr 的循环引用（Memory Leak）问题。

### 虚函数与多态

构造函数不能是虚函数；析构函数通常必须是虚函数（基类指针指向派生类对象销毁时，防止子类析构不被调用导致内存泄漏）。

含有虚函数的类，其实例对象内部会包含一个指向虚函数表（VTable）的指针（vptr），因此 sizeof(类) 会增加一个指针的大小（4/8字节）。

### 关键字区别

const vs constexpr：const 表示运行期只读，constexpr 强制在编译期计算。

static 作用：修饰局部变量延长生命周期至程序结束；修饰全局变量/函数限定作用域在当前源文件（防止命名冲突）；修饰类成员为所有实例共享。