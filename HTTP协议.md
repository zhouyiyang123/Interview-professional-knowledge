# HTTP 协议

HTTP（HyperText Transfer Protocol，超文本传输协议）是 Web 中最常见的应用层协议，主要用于客户端和服务器之间传输超文本、JSON、图片、文件等资源。面试中通常会围绕无状态、连接复用、请求方法、状态码、缓存机制以及 HTTP 版本演进进行考察。

## 一、HTTP 基本概念

### 1. 基于 TCP

HTTP 通常运行在传输层的 TCP 协议之上，默认端口是 80；HTTPS 是在 HTTP 与 TCP 之间加入 TLS/SSL 加密层，默认端口是 443。

### 2. 无状态 (Stateless)

HTTP 协议本身不会记住客户端的历史请求。每一次请求都是独立的，服务器无法仅凭 HTTP 协议知道两次请求是否来自同一个用户。

为了保持登录态或业务状态，常见方案包括：

- **Cookie**：由服务器设置，浏览器后续请求时自动携带。
- **Session**：状态保存在服务器端，客户端通常通过 Cookie 保存 Session ID。
- **Token**：状态或身份凭证由客户端携带，常见于前后端分离和移动端场景。

### 3. 短连接与长连接

- **HTTP/1.0 默认短连接**：每次请求和响应都需要建立一次 TCP 连接，完成后立即断开，连接开销较大。
- **HTTP/1.1 默认长连接**：默认使用 `Connection: keep-alive`，多个 HTTP 请求可以复用同一个 TCP 连接，减少三次握手和四次挥手的开销。

## 二、HTTP 报文结构

HTTP 请求报文和响应报文整体都可以分为四部分，不同部分之间依靠 `\r\n` 分隔。

### 1. 请求报文

请求报文由请求行、请求头、空行和请求体组成。

```http
GET /index.html HTTP/1.1
Host: example.com
User-Agent: curl/8.0

```

请求行格式：

```text
请求方法 请求目标 HTTP版本
```

例如：

```text
GET /index.html HTTP/1.1
```

### 2. 响应报文

响应报文由状态行、响应头、空行和响应体组成。

```http
HTTP/1.1 200 OK
Content-Type: text/html
Content-Length: 12

Hello World!
```

状态行格式：

```text
HTTP版本 状态码 原因短语
```

例如：

```text
HTTP/1.1 200 OK
```

### 3. 报文分隔规则

- **请求行 / 状态行**：以 `\r\n` 结尾。
- **请求头 / 响应头**：每个 Header 单独一行，每行以 `\r\n` 结尾。
- **空行**：一个单独的 `\r\n`，和上一行结尾组合起来就是 `\r\n\r\n`，表示 Header 部分结束。
- **报文主体 (Body)**：位于空行之后，可以为空，也可以是 HTML、JSON、表单数据、文件内容等。

常见面试点：HTTP 解析 Header 时会一直读到 `\r\n\r\n`，它是 Header 和 Body 的分界标志。

## 三、HTTP 请求方法

| 方法 | 语义 | 是否安全 | 是否幂等 |
| --- | --- | --- | --- |
| GET | 获取资源 | 是 | 是 |
| POST | 提交数据或创建资源 | 否 | 否 |
| PUT | 更新或替换完整资源 | 否 | 是 |
| DELETE | 删除资源 | 否 | 是 |
| HEAD | 获取响应头，不返回响应体 | 是 | 是 |
| OPTIONS | 查询服务器支持的请求方法 | 是 | 是 |

**安全性**指请求是否不会修改服务器上的资源。GET、HEAD、OPTIONS 理论上都不应该改变服务器数据。

**幂等性**指同一个请求执行一次和执行多次，对服务器资源产生的最终效果是一样的。例如 PUT 多次更新同一个资源，最终资源状态一致；POST 多次提交则可能创建多条记录，因此通常不幂等。

## 四、URL 编码

URL 编码也叫 Percent-Encoding，用于把 URL 中不能直接出现或容易产生歧义的字符转换成 `%` 加两位十六进制数的形式。

例如：

- 空格通常编码为 `%20`。
- 中文字符会按照字符编码转换成多个百分号编码，例如 `汉` 可以编码为 `%E6%B1%89`。
- 参数中的 `%` 本身必须编码为 `%25`。

### 为什么 `%` 需要编码

`%` 是 URL 编码中的转义前缀。服务端解析 URL 时，看到 `%` 会尝试把它后面的两个字符当成十六进制编码来解析。

如果参数中原本就包含 `%`，但没有进行编码，就可能造成歧义或解析错误。例如：

```text
discount=10%
```

这里的 `%` 后面没有合法的两位十六进制字符，服务端解析时可能报错，常见结果包括 `400 Bad Request`，也可能因为后端处理不当引发 `500 Internal Server Error`。

正确写法是把 `%` 编码为 `%25`：

```text
discount=10%25
```

因此，原始参数 `discount=10%` 经过 URL encode 后应变为 `discount=10%25`。

## 五、常见 HTTP 状态码

### 1. 1xx 信息响应

- `101 Switching Protocols`：协议切换，常见于 HTTP 升级为 WebSocket。

### 2. 2xx 成功

- `200 OK`：请求成功，最常见的成功状态码。
- `204 No Content`：请求成功，但响应报文没有 Body。

### 3. 3xx 重定向

- `301 Moved Permanently`：永久重定向，浏览器和搜索引擎可能缓存新的 URL。
- `302 Found`：临时重定向，资源只是暂时位于其他地址。
- `304 Not Modified`：协商缓存命中，服务器告诉客户端可以继续使用本地缓存。

### 4. 4xx 客户端错误

- `400 Bad Request`：请求报文语法错误或参数格式不正确。
- `401 Unauthorized`：未认证，需要登录或提供身份凭证。
- `403 Forbidden`：服务器理解请求，但拒绝执行，通常是权限不足。
- `404 Not Found`：请求的资源不存在。

### 5. 5xx 服务器错误

- `500 Internal Server Error`：服务器内部错误。
- `502 Bad Gateway`：网关或代理从上游服务器收到无效响应。
- `503 Service Unavailable`：服务器暂时不可用，常见于维护或过载。
- `504 Gateway Timeout`：网关或代理等待上游服务器响应超时。

## 六、HTTP 缓存机制

HTTP 缓存可以分为 **强缓存** 和 **协商缓存**。

### 1. 强缓存

强缓存命中时，浏览器不向服务器发送请求，直接使用本地缓存。

常见响应头：

- **Expires**：HTTP/1.0 使用的缓存过期时间，是一个绝对时间。缺点是依赖客户端本地时间，客户端时间不准会影响缓存判断。
- **Cache-Control**：HTTP/1.1 引入，优先级高于 `Expires`。

常见 `Cache-Control` 指令：

- `max-age=3600`：缓存有效期为 3600 秒。
- `no-cache`：不直接使用强缓存，使用前必须向服务器发起协商校验。
- `no-store`：彻底禁止缓存，请求和响应都不应被缓存。

### 2. 协商缓存

协商缓存会先向服务器发请求校验资源是否变化。如果资源未修改，服务器返回 `304 Not Modified`，客户端继续使用本地缓存；如果资源已修改，服务器返回新的资源内容。

常见响应头和请求头：

- **Last-Modified / If-Modified-Since**：基于资源最后修改时间判断。缺点是时间精度通常到秒，且文件内容没变但修改时间变化时也可能导致缓存失效。
- **ETag / If-None-Match**：基于资源内容或版本生成唯一标识，优先级高于 `Last-Modified`，可以解决时间精度和文件伪修改问题。

## 七、HTTP 版本演进

| 版本 | 核心特性 | 主要问题 |
| --- | --- | --- |
| HTTP/1.0 | 引入请求头、响应头、状态码，支持 GET、HEAD、POST 等方法 | 默认短连接，每次请求都要重新建立 TCP 连接 |
| HTTP/1.1 | 默认长连接，支持管道化、Chunked 传输、Host 头 | 同一连接上的响应必须按顺序返回，存在应用层队头阻塞 |
| HTTP/2 | 二进制分帧、多路复用、HPACK 头部压缩、服务端推送 | 仍然基于 TCP，TCP 丢包会导致整个连接停顿 |
| HTTP/3 | 基于 UDP 实现 QUIC，支持连接迁移、0-RTT 建连，解决 TCP 层队头阻塞 | 协议栈更复杂，对基础设施和兼容性要求更高 |

### 1. HTTP/1.1 队头阻塞

HTTP/1.1 虽然可以复用 TCP 连接，但同一个连接上的请求和响应仍然要按顺序处理。如果前面的请求响应很慢，后面的响应即使已经准备好，也可能被阻塞。

### 2. HTTP/2 多路复用

HTTP/2 将报文拆成二进制帧，不同请求和响应的帧可以在同一个 TCP 连接中交错传输，再根据 Stream ID 重新组装，从而解决 HTTP/1.1 应用层队头阻塞。

但是 HTTP/2 仍然基于 TCP。如果 TCP 层发生丢包，整个 TCP 连接都要等待重传，因此还存在 TCP 层队头阻塞。

### 3. HTTP/3 与 QUIC

HTTP/3 基于 QUIC，而 QUIC 基于 UDP 实现可靠传输、拥塞控制、加密和多路复用。不同 Stream 之间可以相互独立，某个 Stream 丢包不会阻塞其他 Stream。

HTTP/3 的常考优势：

- 解决 TCP 层队头阻塞。
- 建连速度更快，支持 0-RTT。
- 支持连接迁移，网络从 Wi-Fi 切换到移动网络时连接更稳定。

## 八、易错点总结

- HTTP 是应用层协议，HTTP/1.x 和 HTTP/2 通常基于 TCP，HTTP/3 基于 UDP 上的 QUIC。
- HTTP 无状态不代表服务器不能记录用户状态，而是协议本身不保存状态；状态通常依赖 Cookie、Session、Token 实现。
- HTTP 报文中 Header 和 Body 之间通过空行分隔，标志是 `\r\n\r\n`。
- URL 中 `%` 是 Percent-Encoding 的转义前缀，参数里的原生 `%` 必须编码为 `%25`。
- `GET` 通常安全且幂等，`POST` 通常既不安全也不幂等，`PUT` 和 `DELETE` 不安全但幂等。
- `401` 强调未认证，`403` 强调已理解请求但无权限访问。
- `Cache-Control` 优先级高于 `Expires`，`ETag` 优先级高于 `Last-Modified`。
- `no-cache` 不是不缓存，而是使用缓存前必须向服务器校验；真正禁止缓存的是 `no-store`。
- HTTP/2 解决了 HTTP/1.1 的应用层队头阻塞，但没有解决 TCP 层队头阻塞；HTTP/3 通过 QUIC 解决这一问题。
