## 死锁（Deadlock）

死锁是并发编程和多线程开发中的经典问题。

### 一、 发生死锁的原因

死锁是指两个或两个以上的线程（或进程）在执行过程中，因争夺资源而造成的一种互相等待的现象。在没有外力干预的情况下，这些线程都将无法继续推进。

**根本原因：**

- **系统资源不足**：可分配的独占资源数量少于线程请求的总量。

- **并发执行顺序不当**：线程推进的顺序不合理，互相持有对方需要的资源并等待对方释放。

### 二、 死锁的 4 个必要条件

只有当以下 4 个条件**同时满足**时，死锁才会发生；破坏其中任意一个条件，死锁就会被打破：

**1.互斥条件 (Mutual Exclusion)**

含义：资源是独占的，同一时间内某资源只能被一个线程占用。若其他线程请求该资源，只能等待直到释放。

**2.请求与保持条件 (Hold and Wait)**

含义：线程已经保持了至少一个资源，但又提出了新的资源请求，而该资源已被其他线程占有。此时该线程阻塞，但对其已获得的资源保持不放。

**3.不剥夺条件 (No Preemption)**

含义：线程已获得的资源在未使用完之前，不能被其他线程强行剥夺，只能由持有它的线程主动释放。

**4.循环等待条件 (Circular Wait)**

含义：必然存在一个线程—资源的环形等待链。即线程 $T_1$ 等待 $T_2$ 占有的资源，$T_2$ 等待 $T_3$ 占有的资源……$T_n$ 等待 $T_1$ 占有的资源。

### 三、 死锁的解决方案

处理死锁的核心策略分为三种：预防死锁（静态）、避免死锁（动态）、检测与解除死锁。

**1.预防死锁 (Prevent Deadlock) —— 破坏 4 个必要条件之一**

- **破坏“请求与保持”**：采用一次性分配策略。线程在开始运行前，必须一次性申请并获得运行所需的全部资源；只要有一个资源申请不到，就不分配任何资源。

- **破坏“不剥夺”**：如果一个持有了资源的线程在申请新资源被拒绝时，它必须主动释放当前持有的所有资源，之后再重新申请。

- **破坏“循环等待”**（最常用/最推荐）：实行资源按序分配策略。给所有资源编号（如 Lock A 编号为 1，Lock B 编号为 2），规定所有线程必须严格按照资源编号递增的顺序来申请锁（例如必须先锁 A 再锁 B）。

**2.避免死锁 (Avoid Deadlock) —— 动态检测安全性**

在资源分配的过程中，使用算法动态评估本次分配是否会导致系统进入“不安全状态”。

- **银行家算法** (Banker's Algorithm)：最经典的死锁避免算法。在分配资源前，先模拟分配，检查系统是否依然处于安全状态（即能否找到一个线程执行序列，使所有线程都能顺利完成）。若安全才正式分配，否则拒绝分配。

**3.检测与解除死锁 (Detect & Recover)**

允许死锁发生，但系统定期运行检测算法，一旦发现死锁，采取手段恢复：

- **资源剥夺**：挂起某些死锁线程，强行剥夺其占有的资源分配给其他线程。

- **撤销/终止线程**：直接强制终止一个或多个死锁线程（通常按优先级或已执行时间选择），释放其持有的资源。

- **进程回滚：利用检查点**（Checkpoint）机制，将死锁线程回滚到未发生死锁之前的状态重新运行。