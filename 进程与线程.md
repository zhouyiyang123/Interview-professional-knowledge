# 进程与线程

进程与线程是操作系统中最基础、最高频的笔试考点。常见考察方向包括进程和线程的区别、进程状态转换、进程间通信 IPC、线程同步机制，以及在游戏客户端开发中如何避免主线程阻塞。

## 一、进程与线程的基本概念

### 1. 进程

进程是操作系统进行资源分配的基本单位，是程序在计算机上的一次动态执行过程。

进程拥有独立的系统资源，例如：

- 独立的虚拟地址空间。
- 代码段、数据段、堆、栈。
- 文件句柄、Socket 等系统资源。

不同进程之间资源相互隔离。一个进程崩溃，通常不会直接影响其他进程，因此进程的健壮性更好。

### 2. 线程

线程是 CPU 调度和执行的基本单位，也常被称为轻量级进程。

同一进程内的线程共享进程资源，例如：

- 地址空间。
- 全局变量。
- 堆内存。
- 打开的文件和 Socket。

但每个线程也有自己独立的执行上下文：

- 线程栈。
- 程序计数器 PC。
- 寄存器状态。

由于线程共享内存，一个线程崩溃或破坏共享数据，可能导致整个进程崩溃。

### 3. 进程 vs 线程

| 对比维度 | 进程 | 线程 |
| --- | --- | --- |
| 本质区别 | 资源分配的基本单位 | CPU 调度和执行的基本单位 |
| 地址空间 | 拥有独立地址空间 | 共享所属进程的地址空间 |
| 内存开销 | 大 | 小 |
| 切换开销 | 高，需要切换页表、刷新 TLB 等 | 低，主要保存和恢复寄存器、栈等上下文 |
| 通信方式 | 较复杂，需要 IPC，如管道、共享内存、Socket | 较简单，可直接读写全局变量和堆内存 |
| 同步要求 | 跨进程通信需要额外机制 | 共享内存容易产生数据竞争，需要加锁或同步 |
| 健壮性 | 一个进程崩溃通常不影响其他进程 | 一个线程崩溃可能导致整个进程崩溃 |

常见记忆：

- **进程是资源分配单位**。
- **线程是 CPU 调度执行单位**。
- **进程更稳定，线程更轻量**。

## 二、进程状态及其转换

经典操作系统模型中，进程通常有 5 种基本状态：

- **创建状态 (New)**：进程正在被创建。
- **就绪状态 (Ready)**：进程已具备运行条件，正在等待 CPU 调度。
- **执行状态 (Running)**：进程正在 CPU 上执行。
- **阻塞状态 (Blocked)**：进程正在等待某个事件或资源。
- **终止状态 (Terminated)**：进程执行结束或被终止。

状态转换示意：

```text
               +------------+
               |  创建状态  |
               +-----+------+
                     | 提交
                     v
               +------------+   获得 CPU 调度   +------------+
               |  就绪状态  | ----------------> |  执行状态  |
               |  Ready     | <---------------- |  Running   |
               +------------+ 时间片用完/被抢占 +------+-----+
                     ^                                  |
                     | 等待事件发生 / IO 完成             | 请求资源 / 等待 IO
                     |                                  v
                     +---------------------------- +------------+
                                                  |  阻塞状态  |
                                                  |  Blocked   |
                                                  +------------+
```

### 1. 执行态到阻塞态

`Running -> Blocked` 是进程主动引发的状态转换。

触发原因：进程运行过程中需要等待某个外部事件，或请求了暂时无法满足的资源。

典型场景：

- 请求 I/O 操作，例如读取磁盘文件、等待网络 Socket 数据、等待用户输入。
- 申请系统资源失败，例如内存不足。
- 显式调用休眠或等待函数，例如 `sleep()`。
- 等待同步对象，例如信号量 `wait()/P()`、互斥锁 `mutex.lock()`。
- 主线程调用 `join()` 等待子线程结束。

### 2. 阻塞态到就绪态

`Blocked -> Ready` 是被动唤醒。

触发原因：进程等待的事件已经发生，或者资源已经准备就绪。

典型场景：

- I/O 数据读取完毕。
- 休眠时间到期。
- 信号量被释放，例如 `signal()/V()`。
- 锁被释放。

注意：阻塞态不能直接变回执行态，必须先进入就绪队列，重新等待 CPU 调度。

### 3. 执行态到就绪态

`Running -> Ready` 是被动中断。

触发原因：进程本身仍可运行，但 CPU 资源被剥夺。

典型场景：

- 分时系统中时间片用完。
- 出现更高优先级进程，当前进程被抢占。

### 4. 就绪态到执行态

`Ready -> Running` 由调度程序触发。

调度器从就绪队列中选择一个进程，为它分配 CPU 时间片，该进程进入执行态。

## 三、进程间通信 IPC

进程之间地址空间相互隔离，不能像线程一样直接访问彼此的内存，因此需要 IPC 机制。

### 1. 管道

管道用于在进程之间传递字节流。

- **无名管道**：半双工通信，通常只能用于具有亲缘关系的进程，例如父子进程。
- **命名管道 FIFO**：有文件系统中的名字，可以用于任意两个进程之间通信。

特点：简单，但通信方式相对受限。

### 2. 消息队列

消息队列是内核维护的消息链表，进程可以向队列中写入消息，也可以从队列中读取消息。

特点：

- 消息有边界。
- 支持按照消息类型读取。
- 适合传递结构化的小块数据。

### 3. 共享内存

共享内存是最快的 IPC 方式。

多个进程把同一块物理内存映射到自己的地址空间中，可以直接读写同一块内存区域。

特点：

- 速度最快，因为少了内核态和用户态之间的数据拷贝。
- 本身不提供同步能力，需要配合信号量、互斥锁等机制防止并发冲突。

### 4. 信号量

信号量本质上是一个计数器，用于控制多个进程或线程对共享资源的访问。

常见用途：

- 互斥访问共享资源。
- 控制资源数量。
- 进程之间同步。

经典操作：

- `P()` / `wait()`：申请资源，计数减一，不足时阻塞。
- `V()` / `signal()`：释放资源，计数加一，必要时唤醒等待者。

### 5. 信号

信号是一种异步通信机制，用于通知进程发生了某个事件。

常见信号：

- `SIGINT`：终端中断信号，通常由 `Ctrl+C` 触发。
- `SIGKILL`：强制终止进程，不能被捕获或忽略。
- `SIGTERM`：请求进程正常退出。

### 6. Socket

Socket 既可以用于单机跨进程通信，也可以用于网络跨主机通信。

特点：

- 通用性强。
- 可用于本机进程通信，也可用于客户端和服务器通信。
- 是网络编程中的核心机制。

## 四、线程同步机制

同一进程内的线程共享内存，因此通信简单，但也更容易产生数据竞争。

数据竞争指多个线程同时访问同一共享数据，且至少有一个线程在写，如果没有正确同步，就可能导致结果不可预期。

### 1. 互斥锁

互斥锁用于保证同一时刻只有一个线程能访问临界区。

适用场景：

- 多个线程修改共享变量。
- 多个线程访问非线程安全的数据结构。

特点：

- 简单可靠。
- 可能导致阻塞。
- 使用不当可能造成死锁。

### 2. 读写锁

读写锁区分读操作和写操作。

规则：

- 多个读线程可以同时进入。
- 写线程进入时必须独占。
- 读写不能同时进行。

适用场景：读多写少的数据结构，例如配置表、缓存表。

### 3. 条件变量

条件变量允许线程在某个条件不满足时进入阻塞等待，并由其他线程在条件满足后唤醒。

典型场景：

- 生产者消费者模型。
- 任务队列为空时，工作线程休眠等待。
- 新任务到来时，生产者唤醒消费者。

条件变量通常需要配合互斥锁使用。

### 4. 原子操作

原子操作利用 CPU 指令保证某个操作不可被中断。

常见机制：

- CAS。
- 原子自增自减。
- 原子读写。

适用场景：

- 计数器。
- 状态标志。
- 无锁数据结构的基础操作。

优点是轻量，缺点是逻辑复杂时不如锁直观。

## 五、游戏客户端开发中的应用

### 1. 主线程不能阻塞

游戏客户端的主线程通常承担逻辑更新、渲染驱动、输入响应等关键工作。

如果主线程执行同步磁盘 I/O、同步网络请求或长时间等待锁，主线程可能从执行态进入阻塞态，导致：

- 帧率下降。
- 画面卡顿。
- 输入无响应。
- 游戏直接 Freeze。

因此，游戏客户端开发中应避免在主线程执行耗时阻塞操作。

### 2. 异步多线程模式

常见做法是把耗时任务放到后台线程或任务系统中执行。

典型场景：

- **热更新 / 网络层**：在独立 I/O 线程中发送 HTTP 请求、下载资源、读取 Pak 包。
- **资源加载**：后台线程加载纹理、模型、音频等资源，主线程只负责最终提交或渲染相关操作。
- **Job System / Task Graph**：把物理计算、动画更新、寻路等任务拆成大量 Task，分派给线程池并行执行。
- **网络消息处理**：网络线程收包和解包，主线程在安全时机消费消息并更新游戏状态。

### 3. UE / Unity 中的常见思路

- 主线程只做必须在主线程执行的逻辑，例如大部分渲染提交和引擎对象操作。
- 网络、I/O、压缩解压、资源预加载尽量异步化。
- 后台线程不要直接随意修改主线程对象，应通过线程安全队列、任务派发或主线程回调交接数据。
- 多线程共享数据必须明确所有权和同步方式。

## 六、易错点总结

- 进程是资源分配的基本单位，线程是 CPU 调度和执行的基本单位。
- 同一进程内线程共享地址空间和资源，但每个线程有自己的栈、PC 和寄存器状态。
- 进程切换开销通常大于线程切换，因为可能涉及地址空间、页表和 TLB。
- 阻塞态不能直接变为执行态，必须先变成就绪态，再等待调度。
- `Running -> Blocked` 通常是主动等待事件；`Running -> Ready` 通常是时间片用完或被抢占。
- 共享内存是最快的 IPC 方式，但必须配合同步机制。
- 线程共享内存通信方便，但容易出现数据竞争，需要互斥锁、读写锁、条件变量或原子操作。
- 游戏主线程不能做同步 I/O 或长时间等待，否则容易掉帧或卡死。
