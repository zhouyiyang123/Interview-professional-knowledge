# TCP 和 UDP

TCP/UDP 是传输层最常考的两个协议。游戏客户端开发笔试中，常围绕网络分层、封包与解包、TCP 可靠传输、三次握手、四次挥手、粘包拆包、UDP 实时性以及 RUDP/KCP 等方向考察。

## 一、网络分层与封包解包

网络通信本质上是数据在发送端逐层封包，在接收端逐层解包的过程。常见模型包括 **OSI 七层模型** 和 **TCP/IP 四层模型**。

### 1. OSI 七层模型

| 层级 | 名称 | 作用 |
| --- | --- | --- |
| 7 | 应用层 | 提供具体应用协议和业务数据格式 |
| 6 | 表示层 | 处理数据表示、编码、压缩、加密等 |
| 5 | 会话层 | 管理会话建立、保持和释放 |
| 4 | 传输层 | 负责端到端传输，典型协议是 TCP、UDP |
| 3 | 网络层 | 负责 IP 寻址和路由选择 |
| 2 | 数据链路层 | 负责 MAC 寻址、成帧和差错检测 |
| 1 | 物理层 | 负责比特流在物理介质上传输 |

### 2. TCP/IP 四层模型

| TCP/IP 四层 | 对应 OSI 层 | 常见协议 / 数据 |
| --- | --- | --- |
| 应用层 | 应用层、表示层、会话层 | HTTP、WebSocket、DNS、游戏业务协议 |
| 传输层 | 传输层 | TCP、UDP |
| 网络层 | 网络层 | IP、ICMP |
| 网络接口层 | 数据链路层、物理层 | Ethernet、Wi-Fi、MAC、比特流 |

### 3. 封包过程

封包也叫 Encapsulation，是数据发送时从上往下经过协议栈的过程。每一层都会添加自己的控制信息。

以游戏客户端发送“玩家移动”指令为例：

1. **应用层**：生成游戏业务数据，例如 JSON、Protobuf、FlatBuffers，得到原始 `Data`。
2. **传输层**：添加 TCP/UDP 头部，包含源端口和目的端口，形成 `Segment` 或 `Datagram`。
3. **网络层**：添加 IP 头部，包含源 IP 和目的 IP，形成 `Packet`。
4. **数据链路层**：添加 MAC 帧头和帧尾，包含源 MAC、目的 MAC、FCS 校验码，形成 `Frame`。
5. **物理层**：把数据帧转换成比特流，通过网线或 Wi-Fi 传输。

常见数据单位：

```text
应用层 Data -> 传输层 Segment/Datagram -> 网络层 Packet -> 链路层 Frame -> 物理层 Bit
```

### 4. 解包过程

解包也叫 Decapsulation，是数据接收时从下往上经过协议栈的过程。

服务器网卡收到物理信号后：

1. **物理层**：接收比特流。
2. **数据链路层**：校验并剥离 MAC 帧头和帧尾。
3. **网络层**：剥离 IP 头，根据目的 IP 判断是否投递给本机。
4. **传输层**：根据端口号找到对应进程或连接，校验并剥离 TCP/UDP 头。
5. **应用层**：拿到纯净业务 `Data`，投递给游戏逻辑线程处理。

笔试口诀：

- **向下发送**：加头，逐层封装。
- **向上接收**：剥头，逐层解封。
- **数据单位**：应用层 `Data`，传输层 `Segment/Datagram`，网络层 `Packet`，链路层 `Frame`。

## 二、TCP 与 UDP 对比

| 对比维度 | TCP | UDP |
| --- | --- | --- |
| 连接状态 | 面向连接，需要三次握手和四次挥手 | 无连接，发送前不需要建连 |
| 可靠性 | 可靠，支持确认应答、超时重传、去重、按序到达 | 不可靠，尽力而为，可能丢包、乱序、重复 |
| 传输模式 | 面向字节流，没有消息边界 | 面向报文，保留数据边界 |
| 首部开销 | 至少 20 字节 | 固定 8 字节 |
| 传输效率 | 控制机制多，延迟相对更高 | 开销小，速度快，实时性好 |
| 流量控制 | 有，典型机制是滑动窗口 | 无 |
| 拥塞控制 | 有，慢启动、拥塞避免、快重传、快恢复 | 无，需要应用层自行控制 |
| 通信方式 | 点对点单播 | 支持单播、广播、多播 |
| 典型场景 | 文件传输、HTTP、登录、支付、可靠消息 | 游戏同步、音视频、直播、DNS |

## 三、常见应用层协议

应用层协议通常会依赖 TCP 或 UDP 提供传输能力。笔试中常考协议全称、默认端口、使用 TCP 还是 UDP，以及典型用途。

| 协议 | 全称 | 传输层 | 默认端口 | 核心用途 / 考点 |
| --- | --- | --- | --- | --- |
| DNS | Domain Name System | UDP + TCP | 53 | 域名解析；普通查询常用 UDP，大包响应或区域传送使用 TCP |
| Telnet | Telecommunication Network | TCP | 23 | 远程登录；明文传输不安全，已基本被 SSH 22 替代，也常用于测试端口连通性 |
| HTTP | HyperText Transfer Protocol | TCP | 80 | Web、游戏大厅业务；明文传输，无状态 |
| HTTPS | HTTP Secure | TCP | 443 | HTTP + SSL/TLS，加密传输 |
| SMTP | Simple Mail Transfer Protocol | TCP | 25 | 发送邮件 |
| POP3 | Post Office Protocol 3 | TCP | 110 | 接收邮件；本地拉取，通常不强调服务器端同步管理 |
| IMAP | Internet Message Access Protocol | TCP | 143 | 接收和管理邮件；邮件保存在服务器，支持多端双向同步 |

常见补充：

- **DNS 为什么同时用 UDP 和 TCP**：普通查询数据量小，UDP 开销低；当响应过大、发生截断，或进行区域传送时，会使用 TCP。
- **Telnet 为什么不安全**：账号、密码和通信内容都是明文传输，实际生产环境通常使用 SSH。
- **HTTP 与 HTTPS 区别**：HTTPS 在 HTTP 和 TCP 之间加入 SSL/TLS，提供加密、身份认证和完整性保护。

## 四、TCP 核心机制

### 1. 三次握手

三次握手用于建立 TCP 连接，核心目的是确认双方收发能力正常，并同步初始序列号 ISN。

流程：

1. 客户端发送 `SYN`，携带 `seq=x`，进入 `SYN_SENT` 状态。
2. 服务端返回 `SYN + ACK`，携带 `seq=y, ack=x+1`，进入 `SYN_RCVD` 状态。
3. 客户端返回 `ACK`，携带 `ack=y+1`，双方进入 `ESTABLISHED` 状态。

常见追问：为什么不是两次握手？

两次握手只能让客户端确认服务端的接收和发送能力，服务端无法确认客户端的接收能力是否正常，也容易让历史失效连接请求导致服务端误建连接。

### 2. 四次挥手

四次挥手用于关闭 TCP 连接。TCP 是全双工协议，双方的发送方向需要分别关闭，所以通常需要四次。

流程：

1. 主动方发送 `FIN`，进入 `FIN_WAIT_1`。
2. 被动方返回 `ACK`，进入 `CLOSE_WAIT`；主动方收到后进入 `FIN_WAIT_2`。
3. 被动方处理完剩余数据后发送 `FIN`，进入 `LAST_ACK`。
4. 主动方返回 `ACK`，进入 `TIME_WAIT`，等待 `2MSL` 后彻底关闭。

### 3. TIME_WAIT 的作用

主动关闭方会进入 `TIME_WAIT`，等待 `2MSL`。

主要原因：

- **保证最后一个 ACK 能到达对方**：如果最后的 `ACK` 丢失，被动方会重发 `FIN`，主动方仍有机会再次回复 `ACK`。
- **防止旧连接的报文影响新连接**：等待足够长时间，让网络中旧连接残留的报文自然过期。

### 4. 可靠传输机制

TCP 可靠性主要依赖以下机制：

- **序列号**：给字节流编号，支持按序重组和去重。
- **确认应答 ACK**：接收方确认已经收到的数据。
- **超时重传**：发送方超时未收到 ACK，则重发数据。
- **滑动窗口**：在可靠性的基础上提高传输效率，同时实现流量控制。
- **拥塞控制**：通过慢启动、拥塞避免、快重传、快恢复降低网络拥塞风险。

### 5. 粘包与拆包

TCP 是面向字节流的协议，没有消息边界。应用层一次发送的数据，在接收端可能被合并，也可能被拆开。

常见原因：

- 多个小包在发送缓冲区中被合并后一次发送。
- 接收方读取速度、缓冲区大小和发送方写入节奏不一致。
- 大包超过 MTU 或 MSS 后，被底层拆分传输。

常见解决方案：

- **定长包**：每个应用层包固定长度。
- **分隔符**：使用特殊字符标记包结束，例如 `\r\n`。
- **自定义包头 + Length**：游戏开发最常用。包头中写入 Body 长度，接收端先读固定长度包头，再按 Length 读取完整 Body。

示例：

```text
| Length(4 bytes) | Body |
```

## 五、UDP 特点与游戏开发

UDP 是无连接、面向报文的传输层协议。它不保证可靠性，不保证顺序，也不做拥塞控制，但首部小、延迟低、实时性强。

### 1. 为什么游戏战斗常不用 TCP

TCP 可靠但会带来队头阻塞。当前面的 TCP 报文段丢失时，后续已经到达的数据也不能立刻交给应用层，必须等待丢失数据重传并按序补齐。

在实时游戏中，这可能导致：

- 画面卡顿。
- 玩家位置突然顺移。
- 输入响应延迟变高。
- 某些已经过期的状态仍被强行可靠送达。

因此，实时战斗、位置同步、帧同步等场景常优先考虑 UDP 或基于 UDP 的可靠协议。

### 2. UDP 的典型优势

- **无需连接**：不用三次握手，启动传输快。
- **保留消息边界**：一次发送一个 Datagram，接收端按报文读取。
- **首部小**：UDP 头固定 8 字节。
- **低延迟**：没有 TCP 的按序阻塞和复杂拥塞控制。
- **支持广播和多播**：适合部分局域网发现、组播分发等场景。

### 3. RUDP 与 KCP

RUDP 指 Reliable UDP，不是一个固定协议名，而是一类设计思想：底层使用 UDP，应用层自己实现部分可靠传输能力。

常见实现包括 KCP、ENet、自研 RUDP。

核心思想：

- **选择性重传**：丢哪个包就重发哪个包，不必阻塞后续已经到达的包。
- **自定义 ACK 策略**：可以根据业务调整 ACK 频率和延迟。
- **自定义拥塞控制**：可以选择更激进或更保守的策略，在带宽和延迟之间取舍。
- **可靠性分级**：不同消息可以选择可靠、有序、不可靠、最新状态覆盖旧状态等策略。

### 4. 游戏中的常见选择

- **登录、支付、背包、聊天**：更重视可靠性和一致性，常用 TCP 或 HTTP/WebSocket。
- **位置同步、战斗状态、实时输入**：更重视低延迟，常用 UDP、KCP 或其他 RUDP。
- **关键结算结果**：必须可靠到达，通常需要可靠通道或额外确认机制。

## 六、易错点总结

- TCP 是面向连接、可靠、面向字节流；UDP 是无连接、不可靠、面向报文。
- TCP 有粘包拆包问题，本质原因是字节流没有消息边界；UDP 保留报文边界。
- TCP 首部至少 20 字节，UDP 首部固定 8 字节。
- DNS 默认端口是 53，普通查询常用 UDP，大包响应和区域传送使用 TCP。
- HTTP 默认 80，HTTPS 默认 443；SMTP 发送邮件默认 25，POP3 收邮件默认 110，IMAP 默认 143。
- Telnet 默认 23，明文传输不安全；SSH 默认 22，常用于替代 Telnet。
- 三次握手主要是确认双方收发能力并同步初始序列号。
- 四次挥手是因为 TCP 全双工连接的两个方向需要分别关闭。
- `TIME_WAIT` 的核心作用是保证最后的 ACK 可重传，并让旧连接报文自然消失。
- TCP 的可靠性不等于低延迟，丢包时的队头阻塞会影响实时游戏体验。
- RUDP/KCP 的核心是用 UDP 承载数据，在应用层实现重传、ACK、可靠性分级等机制。
