# 排序算法

排序算法是计算机科学中最基础、最常见的算法之一。面试中通常会从时间复杂度、空间复杂度、稳定性、适用场景以及近乎有序数据的选择几个角度考察。

常见排序算法可以分为两类：

- **基于比较的排序**：通过元素之间的大小比较确定顺序，例如冒泡排序、选择排序、插入排序、希尔排序、归并排序、快速排序、堆排序。
- **非比较排序**：不直接依赖元素两两比较，而是利用数据范围、位数或分布特征排序，例如计数排序、桶排序、基数排序。

## 一、常见排序算法对比

| 排序算法 | 最佳时间复杂度 | 平均时间复杂度 | 最坏时间复杂度 | 空间复杂度 | 稳定性 |
| --- | --- | --- | --- | --- | --- |
| 冒泡排序 (Bubble Sort) | O(n) | O(n^2) | O(n^2) | O(1) | 稳定 |
| 选择排序 (Selection Sort) | O(n^2) | O(n^2) | O(n^2) | O(1) | 不稳定 |
| 插入排序 (Insertion Sort) | O(n) | O(n^2) | O(n^2) | O(1) | 稳定 |
| 希尔排序 (Shell Sort) | 取决于增量序列 | 取决于增量序列 | O(n^2) | O(1) | 不稳定 |
| 归并排序 (Merge Sort) | O(n log n) | O(n log n) | O(n log n) | O(n) | 稳定 |
| 快速排序 (Quick Sort) | O(n log n) | O(n log n) | O(n^2) | O(log n) | 不稳定 |
| 堆排序 (Heap Sort) | O(n log n) | O(n log n) | O(n log n) | O(1) | 不稳定 |
| 计数排序 (Counting Sort) | O(n + k) | O(n + k) | O(n + k) | O(k) | 稳定 |
| 桶排序 (Bucket Sort) | O(n + k) | O(n + k) | O(n^2) | O(n + k) | 稳定 |
| 基数排序 (Radix Sort) | O(d(n + k)) | O(d(n + k)) | O(d(n + k)) | O(n + k) | 稳定 |

> 稳定性指的是：相同值的元素在排序前后的相对顺序是否保持不变。保持不变则为稳定排序。

## 二、核心算法解析

### 1. 冒泡排序 (Bubble Sort)

**原理**：重复遍历待排序数组，依次比较相邻两个元素，如果顺序错误就交换位置。每一趟遍历都会把当前未排序部分的最大值移动到末尾。

**特点**：

- 代码简单，适合入门理解排序过程。
- 如果加入 `swapped` 标志位优化，当一趟遍历没有发生交换时，可以提前结束。
- 对完全有序或近乎有序的数据比较友好，最佳时间复杂度为 O(n)。

### 2. 选择排序 (Selection Sort)

**原理**：每一趟从未排序部分中选出最小值，放到当前未排序区间的开头，逐步扩大已排序区间。

**特点**：

- 比较次数基本固定，无法利用数据已有的有序性。
- 交换次数较少，最多交换 n - 1 次。
- 通常是不稳定排序，因为选择最小值并交换时可能破坏相同元素的相对顺序。

### 3. 插入排序 (Insertion Sort)

**原理**：把数组分为已排序区间和未排序区间。每次取出未排序区间的第一个元素，从后向前扫描已排序区间，找到合适位置插入。

**特点**：

- 对小规模数据或近乎有序数据效率很高。
- 当数组基本有序时，元素移动次数很少，时间复杂度可以接近 O(n)。
- 很多工业级混合排序会在子数组规模较小时切换到插入排序。

### 4. 希尔排序 (Shell Sort)

**原理**：可以看作插入排序的改进版。先按照一定增量将数组分组，对每组进行插入排序；随后逐渐缩小增量，直到增量为 1。

**特点**：

- 比直接插入排序更适合处理中等规模、乱序程度较高的数据。
- 时间复杂度和增量序列有关。
- 排序过程中会跨距离移动元素，因此不稳定。

### 5. 归并排序 (Merge Sort)

**原理**：基于分治思想。先递归地将数组拆成两半，直到每个子数组只有一个元素；然后不断合并两个有序子数组，得到最终有序数组。

**特点**：

- 时间复杂度非常稳定，最好、平均、最坏都是 O(n log n)。
- 需要 O(n) 的额外空间辅助合并。
- 稳定性好，常用于链表排序、外部排序和需要稳定性的场景。

### 6. 快速排序 (Quick Sort)

**原理**：选定一个基准值 `pivot`，通过一次划分把数组分成两部分：小于 `pivot` 的放左边，大于 `pivot` 的放右边；然后递归排序左右两部分。

**特点**：

- 平均性能优秀，是非常常用的通用排序算法。
- 如果每次划分都极不均衡，最坏会退化为 O(n^2)。
- 可以通过随机选取 `pivot`、三数取中等方式降低退化概率。
- 原地快速排序通常不稳定。

### 7. 堆排序 (Heap Sort)

**原理**：利用大顶堆或小顶堆。以升序为例，先构建大顶堆，此时堆顶是最大值；然后把堆顶与末尾元素交换，缩小堆范围并重新调整堆，重复直到排序完成。

**特点**：

- 最坏时间复杂度也能保持 O(n log n)。
- 空间复杂度为 O(1)，不需要额外线性空间。
- 由于堆调整会改变相同元素的相对顺序，因此不稳定。

### 8. 计数排序 (Counting Sort)

**原理**：适用于整数。创建计数数组，统计每个值出现的次数，再根据计数结果还原出有序序列。

**特点**：

- 时间复杂度为 O(n + k)，其中 k 是数据范围。
- 当整数范围较小时速度很快，可以突破比较排序 O(n log n) 的下界。
- 只适合整数或可以映射为整数的数据，并且要求数据范围不能太大。

### 9. 桶排序 (Bucket Sort)

**原理**：将数据按照范围映射到不同桶中，每个桶内部单独排序，最后按桶的顺序合并结果。

**特点**：

- 适合数据分布比较均匀的场景。
- 平均时间复杂度可以接近 O(n)。
- 如果数据分布极不均匀，大量元素落入同一个桶，性能可能退化。

### 10. 基数排序 (Radix Sort)

**原理**：按照位数进行排序，例如从个位、十位、百位依次排序。每一位通常使用稳定的计数排序作为子过程。

**特点**：

- 适合整数、字符串等可以按位拆分的数据。
- 依赖每一轮子排序的稳定性。
- 时间复杂度通常写作 O(d(n + k))，其中 d 是位数，k 是每一位的取值范围。

## 三、实际场景选择

### 1. 基本有序或数据量很小

优先选择 **插入排序**。

如果冒泡排序带有提前退出标志位，也可以在基本有序时达到 O(n)，但实际工程中插入排序更常用。

### 2. 通用排序，追求平均性能

优先考虑 **快速排序**。

实际使用时要注意 `pivot` 的选择，避免在已排序或近乎有序数组上因为固定选首元素或尾元素而退化。

### 3. 需要稳定排序，且内存充足

优先考虑 **归并排序**。

归并排序虽然需要 O(n) 额外空间，但时间复杂度稳定，并且可以保持相同元素的相对顺序。

### 4. 关心最坏时间复杂度，且希望节省空间

优先考虑 **堆排序**。

堆排序最坏情况仍然是 O(n log n)，空间复杂度为 O(1)，但它不是稳定排序。

### 5. 已知是整数且范围较小

可以选择 **计数排序**。

如果数据分布比较均匀，也可以考虑 **桶排序**；如果数据可以按位拆分，则可以考虑 **基数排序**。

## 四、面试题：数组近乎有序时选择哪些排序？

如果已知数组大致有序或近乎有序，应该优先选择：

- **插入排序 (Insertion Sort)**
- **带提前退出优化的冒泡排序 (Bubble Sort)**

### 1. 插入排序为什么适合？

插入排序在处理近乎有序数组时，大部分元素只需要少量比较和移动就能找到正确位置。

因此它的时间复杂度可以从平均 O(n^2) 接近优化到 O(n)，空间复杂度仍然是 O(1)。

### 2. 冒泡排序为什么也可以？

如果冒泡排序加入 `swapped` 标志位，一旦某一趟遍历没有发生交换，就可以提前终止。

对于完全有序数组，它只需要一趟扫描即可结束，最佳时间复杂度为 O(n)。

### 3. 为什么其他排序不是最佳？

- **选择排序**：比较次数固定为 O(n^2)，无法利用数组已经接近有序的特点。
- **传统快速排序**：如果固定选择第一个或最后一个元素作为 `pivot`，近乎有序数组可能导致极不均衡划分，最坏退化为 O(n^2)。
- **归并排序**：无论数据是否近乎有序，时间复杂度通常仍然是 O(n log n)，无法直接利用已有顺序优化到 O(n)。
- **堆排序**：同样无法明显利用近乎有序的特征，时间复杂度仍为 O(n log n)。

## 五、易错点总结

- 比较排序的理论下界是 O(n log n)，非比较排序可以在特定条件下突破这个下界。
- 稳定排序不一定更快，不稳定排序也不一定更差，关键取决于业务是否需要保留相同元素的原始相对顺序。
- 快速排序平均很快，但最坏时间复杂度是 O(n^2)，面试中要主动说明优化 `pivot` 的方法。
- 归并排序稳定且时间复杂度稳定，但需要 O(n) 额外空间。
- 堆排序空间复杂度优秀，最坏时间复杂度稳定，但通常缓存友好性不如快速排序。
