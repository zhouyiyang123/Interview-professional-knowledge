# 计算机网络分层

计算机网络分层常考 OSI 七层模型、TCP/IP 四层或五层模型，以及各层的协议数据单元（PDU, Protocol Data Unit）。

核心记忆：**物理比特，链路成帧，网络分组，传输报文段，应用报文**。

## 一、分层模型总览

网络通信本质上是发送端逐层封装，接收端逐层解封装的过程。

### 1. OSI 七层模型

| 层级 | 名称 | 数据单位 | 核心功能 | 常见协议 / 标准 | 典型设备 |
| --- | --- | --- | --- | --- | --- |
| 7 | 应用层 | 报文（Message） | 直接为应用程序提供网络服务 | HTTP、HTTPS、DNS、FTP、SMTP、POP3、DHCP | 应用网关、代理服务器 |
| 6 | 表示层 | 报文（Message） | 数据表示、编码、压缩、加密和解密 | TLS/SSL、JPEG、ASCII | - |
| 5 | 会话层 | 报文（Message） | 建立、管理和释放会话 | RPC、NetBIOS | - |
| 4 | 传输层 | TCP 报文段（Segment）/ UDP 用户数据报（Datagram） | 进程到进程通信，端口寻址，可靠传输或尽力而为传输 | TCP、UDP | 四层交换机、防火墙 |
| 3 | 网络层 | 分组（Packet）/ IP 数据报 | 主机到主机通信，IP 寻址和路由选择 | IP、ICMP、IGMP、OSPF、BGP | 路由器、三层交换机 |
| 2 | 数据链路层 | 帧（Frame） | 相邻节点通信，MAC 寻址，成帧，差错检测 | Ethernet、PPP、ARP、MAC 协议 | 二层交换机、网桥 |
| 1 | 物理层 | 比特（Bit）/ 比特流 | 在物理介质上传输原始比特流 | RJ45、IEEE 802.3 物理部分、EIA-232 | 集线器、中继器、网线、光纤 |

### 2. TCP/IP 四层模型

| TCP/IP 四层 | 对应 OSI 层 | 数据单位 | 常见协议 |
| --- | --- | --- | --- |
| 应用层 | 应用层、表示层、会话层 | Message | HTTP、HTTPS、DNS、FTP、SMTP |
| 传输层 | 传输层 | Segment / Datagram | TCP、UDP |
| 网络层 | 网络层 | Packet / IP Datagram | IP、ICMP、IGMP、OSPF、BGP |
| 网络接口层 | 数据链路层、物理层 | Frame / Bit | Ethernet、Wi-Fi、PPP |

### 3. TCP/IP 五层模型

教学和笔试中也常用五层模型，把网络接口层拆成数据链路层和物理层。

```text
应用层     Message
传输层     Segment / Datagram
网络层     Packet / IP Datagram
数据链路层 Frame
物理层     Bit
```

## 二、各层数据单位与功能

### 1. 物理层

物理层的数据单位是 **比特（Bit）**，负责通过双绞线、光纤、无线电波等介质传输原始比特流。

核心功能：

- 定义电气特性、机械特性和接口标准。
- 完成数字信号与电信号、光信号、无线信号之间的转换。
- 透明传输 0 和 1。

典型设备：

- 集线器（Hub）。
- 中继器（Repeater）。
- 网线、光纤。

### 2. 数据链路层

数据链路层的数据单位是 **帧（Frame）**。

它负责把网络层交下来的分组封装成帧，并在同一链路或同一局域网内实现相邻节点之间的数据传输。

核心功能：

- 成帧。
- MAC 地址寻址。
- 差错检测，如 CRC。
- 流量控制。
- 介质访问控制。

典型协议和设备：

- 协议：Ethernet、PPP、ARP、MAC 协议。
- 设备：二层交换机、网桥。

注意：ARP 用来根据 IP 地址解析 MAC 地址，常被放在数据链路层和网络层之间考查。

### 3. 网络层

网络层的数据单位是 **分组（Packet）**，在 IP 协议中也叫 **IP 数据报**。

它负责把分组从源主机路由转发到目的主机，实现主机到主机的通信。

核心功能：

- IP 地址寻址。
- 路由选择。
- 分组转发。
- 跨网络通信。

典型协议和设备：

- 协议：IPv4、IPv6、ICMP、IGMP、OSPF、BGP。
- 设备：路由器、三层交换机。

### 4. 传输层

传输层的数据单位取决于协议：

- TCP：报文段 / 数据段（Segment）。
- UDP：用户数据报（Datagram）。

传输层负责端到端通信，更准确地说是应用进程到应用进程的通信。

核心功能：

- 端口寻址。
- 复用与分用。
- 差错控制。
- 流量控制。
- 拥塞控制。
- 可靠传输或尽力而为传输。

典型协议：

- TCP：面向连接、可靠传输、面向字节流。
- UDP：无连接、尽力而为传输、面向报文。

### 5. 应用层

应用层的数据单位通常叫 **报文（Message）** 或应用数据。

它直接面向应用程序，为用户提供具体网络服务。

典型协议：

- HTTP / HTTPS：Web 服务。
- DNS：域名解析。
- FTP：文件传输。
- SMTP / POP3 / IMAP：电子邮件。
- DHCP：动态分配 IP 地址。

## 三、封包与解包

### 1. 封包过程

封包也叫 Encapsulation，是发送端数据从上往下经过协议栈的过程。每一层都会添加自己的控制信息。

以客户端发送一条业务消息为例：

1. **应用层**：生成业务数据，得到 `Message`。
2. **传输层**：添加 TCP 或 UDP 头部，形成 `Segment` 或 `Datagram`。
3. **网络层**：添加 IP 头部，形成 `Packet`。
4. **数据链路层**：添加 MAC 帧头和帧尾，形成 `Frame`。
5. **物理层**：把帧转换成 `Bit` 比特流，通过物理介质传输。

```text
Message -> Segment / Datagram -> Packet -> Frame -> Bit
```

### 2. 解包过程

解包也叫 Decapsulation，是接收端数据从下往上经过协议栈的过程。

1. **物理层**：接收比特流。
2. **数据链路层**：校验并剥离帧头和帧尾。
3. **网络层**：剥离 IP 头，根据目的 IP 判断是否投递给本机。
4. **传输层**：根据端口号找到对应进程或连接，并剥离 TCP/UDP 头。
5. **应用层**：拿到业务报文，交给应用程序处理。

## 四、寻址方式

| 协议层 | 寻址方式 | 含义 |
| --- | --- | --- |
| 数据链路层 | MAC 地址 | 定位同一链路中的相邻节点 |
| 网络层 | IP 地址 | 定位网络中的主机 |
| 传输层 | 端口号 | 定位主机上的进程或服务 |
| 应用层 | URL / 域名 / 资源路径 | 定位具体应用资源 |

常见说法：

- 数据链路层：节点到节点。
- 网络层：主机到主机。
- 传输层：进程到进程。
- 应用层：应用到应用。

## 五、笔试速记

| 协议层 | PDU 数据单位 | 核心寻址方式 | 典型设备 |
| --- | --- | --- | --- |
| 物理层 | 比特（Bit） | - | 集线器、中继器 |
| 数据链路层 | 帧（Frame） | MAC 地址 | 二层交换机、网桥 |
| 网络层 | 分组（Packet）/ IP 数据报 | IP 地址 | 路由器、三层交换机 |
| 传输层 | TCP Segment / UDP Datagram | 端口号 | 四层交换机、防火墙 |
| 应用层 | 报文（Message） | URL / 域名 | 应用网关、代理服务器 |

一句话口诀：

```text
物理比特，链路成帧，网络分组，传输数据段，应用为报文。
```

