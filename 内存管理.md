# 内存管理

## 一、虚拟内存 vs 物理内存

### 1. 物理内存

物理内存也叫实存，指内存条上真实存在的存储空间。CPU 最终访问的都是物理地址对应的内存单元。

如果只使用实存管理，程序通常需要整体装入内存，并在运行期间常驻内存。物理内存不足时，程序就无法顺利运行。

### 2. 虚拟内存

虚拟内存是操作系统给进程提供的一套逻辑地址空间。进程看到的是虚拟地址，真正访问内存时再通过硬件和操作系统转换为物理地址。

虚拟内存的四个核心特征：

- **离散分配**：程序被切分成页或段，不要求在物理内存中连续存放。
- **多次性**：程序运行前不需要全部装入内存，只需要先装入当前运行必需的部分。
- **对换性**：运行过程中可以把暂时不用的页面换出到磁盘，需要时再换入内存。
- **虚拟性**：逻辑上给进程提供一个可能大于实际物理内存的地址空间。

## 二、实存管理 vs 虚存管理

| 维度 | 实存管理（基本分页 / 分段 / 分区） | 虚存管理（请求分页 / 请求分段） |
| --- | --- | --- |
| 装入要求 | **一次性**：程序必须全部装入内存才能运行 | **多次性**：先装入一部分，其余按需调入 |
| 驻留要求 | **驻留性**：运行期间一直留在内存 | **对换性**：暂不用的换出到磁盘，需要时调入 |
| 地址转换 | 也存在逻辑地址到物理地址的映射 | 存在地址映射，并可能触发缺页中断 |
| 物理连续性 | 分区可连续，基本分页可不连续 | 天然支持不连续存放 |
| 理论支撑 | 无特殊依赖 | **局部性原理** |

局部性原理包括：

- **时间局部性**：刚访问过的数据或指令，短时间内可能再次被访问。
- **空间局部性**：访问某个地址后，附近地址也很可能被访问。

## 三、地址转换与 MMU

程序中使用的是逻辑地址，也叫虚拟地址；内存条上真实访问的是物理地址。

**MMU（Memory Management Unit，内存管理单元）** 是 CPU 内部负责地址转换的硬件模块。程序执行时，MMU 会根据页表、段表等结构，把虚拟地址实时转换成物理地址。

典型分页地址转换：

```text
虚拟地址 = 页号 + 页内偏移

页号 -> 查询页表 -> 得到页框号
物理地址 = 页框号 + 页内偏移
```

### TLB

TLB（Translation Lookaside Buffer，快表）是 MMU 中的高速缓存，用来保存最近使用过的页号到页框号的映射。

笔试常考访问次数：

- **无 TLB**：访问一次数据通常需要 2 次访存，即查页表 1 次、读数据 1 次。
- **TLB 命中**：访问一次数据通常只需要 1 次访存，即直接得到页框号后读数据。
- **TLB 未命中**：先查页表，再更新 TLB，最后访问数据。

## 四、分页与分段

分页和分段都是非连续内存管理方式，但出发点不同。

| 维度 | 分页管理（Paging） | 分段管理（Segmentation） |
| --- | --- | --- |
| 划分目的 | 系统管理需要，提高内存利用率 | 用户或程序逻辑需要，按代码段、数据段、栈段等划分 |
| 大小 | 页大小固定，常见为 4KB | 段长度不固定 |
| 地址空间 | 一维地址空间 | 二维地址空间，需要段号和段内偏移 |
| 碎片类型 | 页内碎片，也叫内部碎片 | 段间碎片，也叫外部碎片 |

### 段页式存储管理

段页式存储管理结合分页和分段的优点：先按逻辑划分成段，再把每个段分页。

一次数据访问通常需要：

```text
查段表 -> 查页表 -> 读数据
```

因此在没有快表优化时，段页式访问一次数据可能需要 3 次访存。

## 五、请求分页与缺页中断

请求分页是虚拟内存最常见的实现方式。

程序运行时，只把当前需要的页面放入内存。CPU 访问某个页面时，如果该页不在物理内存中，就会触发 **缺页中断（Page Fault）**。

缺页中断的大致处理流程：

```text
访问页面
  -> 页表发现页面不在内存
  -> 触发缺页中断
  -> 操作系统判断访问是否合法
  -> 从磁盘把页面调入内存
  -> 必要时执行页面置换
  -> 更新页表 / TLB
  -> 重新执行被中断的指令
```

注意：缺页中断不是普通函数调用，而是由硬件和操作系统共同完成的异常处理过程。

## 六、页面置换算法

当发生缺页中断，并且物理内存已经没有空闲页框时，操作系统必须选择一个页面换出到磁盘。

### 1. OPT 最佳置换算法

OPT 会淘汰以后再也不用，或者最长时间内不会再被访问的页面。

它是理论最优算法，但实际系统无法预知未来，所以通常只作为性能比较基准。

### 2. FIFO 先进先出算法

FIFO 淘汰最早进入内存的页面。

它实现简单，但可能出现 **Belady 异常**：分配给进程的物理块数增加，缺页次数反而增加。

### 3. LRU 最近最久未使用算法

LRU 淘汰过去最长时间没有被访问的页面。

它基于局部性原理，效果通常较好，但精确实现需要记录访问历史，硬件和系统开销较高。

### 4. Clock 时钟置换算法

Clock 算法为每个页面设置访问位，并用一个指针循环扫描页面：

- 访问位为 0：选择该页淘汰。
- 访问位为 1：清零访问位，给它第二次机会，指针继续后移。

Clock 是 LRU 的近似实现，兼顾性能和实现成本。

## 七、内存抖动与工作集

**内存抖动（Thrashing）** 也叫颠簸，指系统频繁发生缺页，大量时间都花在磁盘 I/O 和页面换入换出上，真正执行程序指令的时间很少。

根本原因通常是：分配给进程的物理块太少，不能容纳进程当前运行所需的工作集。

**工作集（Working Set）** 是进程在某段时间窗口内频繁访问的页面集合。只要物理块数量能覆盖工作集，缺页率通常就会明显下降。

常见缓解方式：

- 给进程分配更多物理块。
- 减少并发运行的进程数量。
- 使用更合理的页面置换算法。
- 控制程序访问局部性，避免大范围随机访问。

## 八、笔试速记

- 看到 **虚拟内存**：想到多次性、对换性、离散性、虚拟性。
- 看到 **缺页中断**：想到请求分页、磁盘换入、页面置换。
- 看到 **4KB、内部碎片、一维地址**：想到分页。
- 看到 **逻辑模块、二维地址、外部碎片**：想到分段。
- 看到 **MMU**：想到硬件地址转换。
- 看到 **TLB**：想到页表映射缓存和访存次数优化。
- 看到 **FIFO**：小心 Belady 异常。
- 看到 **频繁换页、CPU 利用率低、磁盘 I/O 高**：想到内存抖动。

